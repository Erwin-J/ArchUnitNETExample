using Domain;

namespace Infrastructure
{
    public class AnotherExternalClient
    {
        public void SendToExternalPartner()
        {
            // Comment out to fail the associated test.
            //var domainClass = new DomainClass();
        }
    }
}
